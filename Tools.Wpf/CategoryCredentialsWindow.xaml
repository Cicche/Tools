<Window x:Class="Tools.Wpf.CategoryCredentialsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Credenziali Categoria" Height="250" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">Categoria</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Name="TxtCategory" IsReadOnly="True" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">User</TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" Name="TxtUser" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">Password</TextBlock>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <PasswordBox Name="TxtPassword" PasswordChanged="TxtPassword_OnPasswordChanged"/>
                <TextBox Name="TxtPasswordVisible" Visibility="Collapsed" TextChanged="TxtPasswordVisible_OnTextChanged"/>
            </Grid>
            <CheckBox Grid.Column="1" Name="ChkShowPassword" Margin="8,0,0,0" VerticalAlignment="Center" Checked="ChkShowPassword_OnToggled" Unchecked="ChkShowPassword_OnToggled">Mostra</CheckBox>
        </Grid>

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">Conferma</TextBlock>
        <Grid Grid.Row="3" Grid.Column="1" Margin="0,0,0,8">
            <PasswordBox Name="TxtConfirmPassword" PasswordChanged="TxtConfirmPassword_OnPasswordChanged"/>
            <TextBox Name="TxtConfirmPasswordVisible" Visibility="Collapsed" TextChanged="TxtConfirmPasswordVisible_OnTextChanged"/>
        </Grid>

        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Name="LblValidation" Foreground="#B00020" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Margin="0,8,8,0" Click="Cancel_Click">Annulla</Button>
            <Button Width="90" Margin="0,8,0,0" Click="Confirm_Click">Conferma</Button>
        </StackPanel>
    </Grid>
</Window>
